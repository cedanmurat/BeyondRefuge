<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recodedcedan.beyondrefuge">

    <application
        android:allowBackup="true"
        android:icon="@drawable/brlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/blogoround"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RacismActivity" />
        <activity android:name=".AntiRefugeActivity" />
        <activity android:name=".NationalismActivity" />
        <activity android:name=".SexisimActivity" />
        <activity android:name=".SelfPacedLearningActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".ContactUsActivity" >
     </activity>
    </application>

</manifest>